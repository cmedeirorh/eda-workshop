---
- name: Respond to Zabbix Events
  hosts: all
  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        port: 35001

  rules:
    - name: Zabbix Alert - SELinux Change
      condition: event.payload.trigger_name == "Security Alert - selinux-config has been changed"
      action:
        run_job_template:
          name: "LINUX Workshop - SELinux Config"
          organization: Default
          job_args:
            extra_vars:
              _hosts: "{{ event.payload.host_host }}"
